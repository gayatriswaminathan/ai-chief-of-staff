# VIP list — messages from these people never slip out of the brief,
# and NOTHING addressed to them is ever auto-sent (hard rule, enforced in code).
# Pattern: De Jesus "NEVER_AUTO_SEND" + Chawla VIP list.
vips:
  - name: Marcus Webb
    role: Managing Director (Regina's manager)
    never_auto_send: true
    always_surface: true
  - name: James Whitfield
    role: Model Risk Governance liaison
    never_auto_send: true
    always_surface: true
  - name: Katherine Boyd
    role: Finance Controller (regulatory sign-offs)
    never_auto_send: true
    always_surface: true
